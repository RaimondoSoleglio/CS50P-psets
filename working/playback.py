# store the inputted string in a variable called sentence
sentence = input("Please write at least a few words with spaces in between: ")

# use .replace() method to replace spaces with dots and then print
print(sentence.replace(" ", "..."))